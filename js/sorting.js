class Sort {
  constructor() {
    self.rects = document.getElementsByClassName("rect_stroke")
  }

  getHeights() {
    return Array.from(rects).map(rect => rect.height.baseVal.value);
  }

  quickSort() {
      
  }
}